<section id="articles" style="padding-top:70px" class="row">
  <div class="col-md-10 col-md-offset-1">
    <div class="headline">
      <h2>Recent Articles</h2>
    </div>

    <p class="lead">
      I write articles on all sorts of topics, which you can find over at the
      <a href="https://blog.woss.name/">Wossname Industries</a> publication on
      Medium and, occasionally, the odd additional article on
      <a href="https://medium.com/@mathie">my own profile @ Medium</a>.  I'll
      endeavour to post relevant articles over here, too.
    </p>

    <div class="row">
      <% data.articles.each do |id, article| %>
        <div class="col-sm-6">
          <div class="thumbnails thumbnail-style thumbnail-kenburn">
            <div class="thumbnail-img">
              <div class="overflow-hidden">
                <img class="img-responsive" width="540" height="360" src="<%= image_path(article.image) %>" alt="<%= article.title %>" />
              </div>
              <a class="btn-more hover-effect" href="<%= article.url %>">read more +</a>
            </div>
            <div class="caption">
              <h3>
                <%= link_to article.title, article.url, class: 'hover-effect' %>
              </h3>
              <p>
                <%= article.description %>
              </p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>
